# Site before Machinery of Magic

Snapshot taken 2026-09-09 before the redesign, from working tree at commit e87e7b9.
Includes the existing source, public assets, Astro configuration, package manifest,
lockfile, and README, including local website experiments as they stood.

This directory is not published. TypeScript excludes it. Runtime files, credentials,
node_modules, Git metadata, and the prospecting application are not included.

To inspect it independently, copy this directory to a separate working directory,
install the locked dependencies, and run the scripts in package.json.
