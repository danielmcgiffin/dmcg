PLAZA REPAIR

Use plaza-repaired-full-canvas.png: 1024 x 558 RGBA, positioned at x=0, y=0.
Import beneath all upper groups and above Background. Hide the old plaza. Do not use its old cropped offset (172,287).

Open preview.html to lift/hide upper layers and toggle before/after.
plaza-before-after.png: all upper layers hidden.
plaza-lifted-preview.png: upper layers moved upward 160 pixels.

The completed pavement and rear perimeter are inferred. The original front curb alignment is retained. Stray old object fragments and ground shadows are removed. Other layers and background cutouts are unchanged; originals are untouched.

Optional plaza-source-preserving.png keeps all 32,234 original opaque plaza pixels exactly, including old fragments and shadows, and fills only gaps. Both PNG variants use offset (0,0).

Method: built-in image generation supplied pavement texture; local Pillow processing excluded the painted checkerboard, mapped it to the plaza footprint and verified opacity throughout its interior.
Prompt intent: Reconstruct uninterrupted warm beige paving beneath all objects, matching the original isometric perspective and fine ink site edge. No new objects or object-shaped holes.
