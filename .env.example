# Optional override. Production builds use the public GA4 ID in src/config.ts.
# Local development does not load GA4 unless this is set.
PUBLIC_GA_MEASUREMENT_ID=

# Evidence/testimonials stay unpublished unless both are set and
# src/content/proof/ contains verified entries.
SHOW_PROOF=false
PROOF_HEADLINE=
